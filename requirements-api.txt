# =============================================================================
# API PRODUCTION DEPENDENCIES ONLY
# Minimal requirements for Render deployment
# =============================================================================

# Core ML (ONLY XGBoost - CatBoost not needed in production)
numpy==1.24.3
pandas==2.0.3
xgboost==1.7.6
joblib==1.3.2

# API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# HTTP Requests
requests==2.31.0

# Utilities
python-dotenv==1.0.0
PyYAML==6.0.1

# Data handling
pyarrow==14.0.1
EOFcat > requirements-api.txt << 'EOF'
# =============================================================================
# API PRODUCTION DEPENDENCIES ONLY
# Minimal requirements for Render deployment
# =============================================================================

# Core ML (ONLY XGBoost - CatBoost not needed in production)
numpy==1.24.3
pandas==2.0.3
xgboost==1.7.6
joblib==1.3.2

# API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# HTTP Requests
requests==2.31.0

# Utilities
python-dotenv==1.0.0
PyYAML==6.0.1

# Data handling
pyarrow==14.0.1
EOFcat > requirements-api.txt << 'EOF'
# =============================================================================
# API PRODUCTION DEPENDENCIES ONLY
# Minimal requirements for Render deployment
# =============================================================================

# Core ML (ONLY XGBoost - CatBoost not needed in production)
numpy==1.24.3
pandas==2.0.3
xgboost==1.7.6
joblib==1.3.2

# API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# HTTP Requests
requests==2.31.0

# Utilities
python-dotenv==1.0.0
PyYAML==6.0.1

# Data handling
pyarrow==14.0.1
